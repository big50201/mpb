import React from "react";

const Channels = () => {
  return (
    <div className="channels" style={{ padding: 24, background: '#fff', minHeight: 600 }}>
      channels
    </div>
  );
};

export default Channels;